window.APP_CONFIG = window.APP_CONFIG || {
  // Set this to your deployed worker URL, for example:
  // AI_ENDPOINT: 'https://proofadvice-ai-worker.your-subdomain.workers.dev',
  AI_ENDPOINT: '',
  AI_TIMEOUT_MS: 30000
};
